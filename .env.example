# Optional per-machine overrides (do NOT commit values here — this file is
# local-only and intentionally not tracked; UAC falls back to defaults).
#
# UAC_NOTICER_INBOX="$HOME/.letta/agents/<your-agent-uuid>/memory/reference/inbox"
# UAC_SOV_HOST="store-host"
# LETTA_AGENT_ID="<your-letta-agent-uuid>"
# UAC_REPO_DIR="$HOME/unified-agent-context"
